# Sarah Voice Agent - Production Dependencies (OOM-Safe for Render Free)
# 
# Core Framework
fastrtc>=0.0.34
# No extras [stt, tts] to save 800MB+ of space and 500MB+ of RAM

# AI/ML
langchain>=0.1.0,<0.3.0
langchain-groq>=0.1.0
langgraph>=0.0.1

# Vector Search (Note: Superlinked is heavy, using optimized config)
superlinked>=0.1.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0

# Text Processing
inflect>=7.0.0

# Environment & Config
python-dotenv>=1.0.0

# Audio Processing (API-based replacements for memory safety)
edge-tts>=6.1.0
av>=12.0.0  # For decoding Edge-TTS streams

# Web UI
gradio>=4.0.0
# Persistent Memory Storage
redis[hiredis]>=5.0.0

# Telephony
twilio>=8.0.0
fastapi>=0.100.0
uvicorn>=0.20.0

# Additional SDKs
groq>=0.5.0

# WebRTC VAD for intelligent speech detection
webrtcvad-wheels>=2.0.10
